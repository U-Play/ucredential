<% users.each_slice(4) do |group| %>
  <div style="page-break-before:always;display:block;clear:both">
    <% group.each do |user| %>
      <%= render 'ucredential/card', user: user, host: host %>
    <% end %>
  </div>
<% end %>
